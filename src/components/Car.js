import React, { Component } from "react";

class Car extends Component {
  render() {
    return <h1>Hello car</h1>;
  }
}

export default Car;

// Functional components are simple functions, receiving props and returning a declaration.
// Advantage of functional component is doesn't have this keyword
// Solution without using state
// states

// Maitian their own private data state
// Complex UI logic
// Provide licycle hooks

// JSX? JavaScript XML Extension to the javaScipt language Syntax
// Write XML like code for elements and components

// export default is used to export a single value as the default export, while export with named exports is used to export multiple values as named exports. The choice between the two syntaxes depends on your specific needs for code organization and reusability.
